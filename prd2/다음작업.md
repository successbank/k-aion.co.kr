# Kaion 4-Stage Workflow — 다음 작업 가이드

> **작성**: 2026-04-15 체크포인트
> **현재 상태**: Stage 1+2+3 완료 (100%), Stage 4 17% (16/92 항목 PASS)
> **마지막 commit**: `a5ed959` Stage 4 batch 4
> **재개 시점**: 신선한 컨텍스트에서 시작 권장

---

## 0. 빠른 재개 명령

새 세션 시작 시 이 순서로:

```bash
cd /data/successbank/projects/kaion

# 1. 진행 상태 확인
git log --oneline -10
grep -c "✅ PASS (2026-04-15" prd2/*.md  # 16건이 표시되어야 함

# 2. 자율 결정 이력
cat prd2/요청.md | head -50

# 3. 본 가이드 + plan
cat prd2/다음작업.md  # 이 파일
cat /home/successbank/.claude/plans/quirky-watching-thacker.md
```

또는 `@PM팀 Stage 4 재개` 한 줄로 호출하면 페르소나가 위 흐름을 자동 진행합니다.

---

## 1. 즉시 처리할 우선순위 작업 (Top 5)

다음 세션 시작 시 이 순서대로 진행 권장:

### 🔥 1순위: 런타임 검증 (가장 시급)

이번 세션에서 16개 변경이 모두 **정적 검증만** 수행. 누적 런타임 검증 부재가 다음 작업의 위험.

**작업**:
```bash
# 컨테이너 재시작 (husky, supertest, event-emitter 새로 install)
docker-compose down
docker-compose up -d --build

# DB 마이그레이션 적용 (DEV2-013)
docker exec kaion_backend pnpm prisma migrate deploy

# 런타임 검증 항목 (각각 PASS/FAIL 기록)
# (a) HTTP 로그 활성화 확인
docker logs --tail 100 kaion_backend | grep "HTTP"

# (b) Nginx /health
curl -i http://localhost:5667/health

# (c) 보상플랜 매트릭스
curl http://localhost:5667/api/v1/compensation-plan/overview | jq '.products[]'
# 고주파 salePrice가 3300000인지 확인

# (d) Swagger 색상
# 브라우저: http://localhost:5667/api → topbar 빨간색

# (e) /organization 신 4등급 (각 등급 회원 로그인)
# (f) menu.config 신 4단계 (각 등급 메뉴 표시)
```

**결과 기록**: 16개 PASS 항목 중 런타임 실패 발견 시 해당 항목을 PASS → FAIL 재마킹 + `prd2/evidence/{팀}/{ITEM-ID}/runtime.log` 추가.

**예상 소요**: 30분~1h

---

### 🚀 2순위: DEV1-004 JWT 가드 실구현 (★ 8 unblock)

**왜 최우선**: 단일 작업이 다른 8개 항목을 cascade unblock. P0 진척률이 4/22 → 12/22로 한 번에 점프 가능.

**현재 상태** (개발1팀_검증체크리스트.md 참조):
- `auth/guards/jwt-auth.guard.ts` `canActivate` 메서드가 TODO stub
- `auth/guards/roles.guard.ts` `canActivate` 메서드가 TODO stub
- `members.controller.ts` 일부 라우트만 가드 적용, 4건 누락:
  - `getAllOrganizationTrees` (line 830) — `@Roles(ADMIN)`만, `@UseGuards` 없음 → **전체 조직 공개**
  - `resetPassword` (line 923) — `JwtAuthGuard`만, `RolesGuard` 없음 → **계정 탈취 경로**
  - `promoteIfEligible` (line 748), `suggestTeamLine` (line 875) — 가드 없음

**작업 방식**: subagent 1개 (~1h)

**Subagent 프롬프트 핵심**:
```
You are 오지훈 (개발1팀 보안 전문가) executing DEV1-004.

REQUIRED READING:
1. /data/successbank/projects/kaion/prd2/개발1팀_검증체크리스트.md (DEV1-004 항목)
2. /data/successbank/projects/kaion/prd/개발1팀_파악된내용.md (Critical #1)
3. apps/backend/src/auth/guards/jwt-auth.guard.ts
4. apps/backend/src/auth/guards/roles.guard.ts
5. apps/backend/src/members/members.controller.ts (4 누락 라우트)

TASK:
1. JwtAuthGuard.canActivate() 실구현 (TODO stub 제거)
2. RolesGuard.canActivate() 실구현
3. members.controller.ts 4 라우트에 @UseGuards(JwtAuthGuard, RolesGuard) + @Roles(...) 적용
4. 3-증거 수집 (test-result.log, manual.md, diff.patch)
5. 체크리스트 PASS 마킹
6. 8 unblock된 항목들 식별하고 보고

CONSTRAINTS:
- 자율 처리 directive 적용 (prd2/요청.md 기록)
- raw SQL/v2 ★ 마커 보존
```

**예상 효과**:
- DEV1-004 PASS
- 자동 unblock: DEV1-001/002/009 + DEV2-006/007/008/009/010 + QA-005/010 = 8 항목 시작 가능

---

### 📐 3순위: DEV3-001 + DEV3-002 verify-feature/integration-check Skill (전사 허브)

**왜 중요**: 모든 팀의 Stage 4 검증 자동화 허브. 작성 후 Stage 4 나머지 항목들이 더 빨리 진행됨.

**현재 상태**:
- `.claude/skills/` 폴더 자체 부재
- 페르소나 카드(이정우)에 약속된 Skill 미구현
- DEV3-004 (settings.json 골격)은 이미 PASS — Stop hook을 추가할 entry 준비됨

**작업**: subagent 1개 (~1h)

**Subagent 프롬프트 핵심**:
```
You are 이정우 (개발3팀 테스트 Skill) executing DEV3-001 + DEV3-002.

TASK:
1. .claude/skills/verify-feature/SKILL.md 작성
   - Description: "기능 단위 자동 검증 — 서버 기동 + API 호출 + 로그 수집"
   - Trigger: feature 단위 작업 완료 시
2. .claude/skills/integration-check/SKILL.md 작성
   - Description: "누적 통합 검증 — 기존 기능 깨짐 확인"
3. 두 Skill의 실행 스크립트 (bash or Node)
4. .claude/settings.json의 Stop hook에 verify-feature 연결
5. 3-증거 수집

REFERENCE:
- prd/개발3팀_파악된내용.md §3 [CR-3-01]
- 기존 .claude/skills/ 패턴 (다른 plugin Skill 참고)
- DEV3-004의 settings.json 골격
```

---

### 🎨 4순위: DESIGN-001/002/003 admin UI 구 6종 정리 (medium inline batch)

**작업 범위**: 3개 admin 페이지의 구 6종 보너스 / 5등급 UI 잔재 제거.

**파일**:
- `app/admin/compensation-plan/page.tsx` (DESIGN-001)
- `app/admin/bonus-simulator/page.tsx` + `services/bonus-simulator.service.ts` + `CommissionRateCompareTab.tsx` (DESIGN-002)
- `app/admin/organization/page.tsx` (DESIGN-003)

**작업 방식**: medium inline (~30~60분, 메인 세션)
또는 1 subagent (디자인팀 김서현 + 개발2팀 강지훈 협업)

**참고**: 이번 세션에서 DESIGN-004 (사용자 /organization)와 DESIGN-005 (menu.config)는 이미 PASS. 같은 패턴으로 진행 가능.

---

### ⚙️ 5순위: MON-006 컨테이너 healthcheck 5종

**작업 범위**: docker-compose.yml의 backend/frontend/nginx/redis/adminer 5개 컨테이너에 healthcheck 추가.

**작업 방식**: medium inline (~30분), 컨테이너별 healthcheck 명령 careful 작성:
- backend: `wget --spider http://localhost:3001/api/health` (Alpine wget 가용성 확인 필요)
- frontend: `wget --spider http://localhost:3000`
- nginx: `wget --spider http://localhost/health` (MON-004 fix 후 가능)
- redis: `redis-cli -a $REDIS_PASSWORD ping`
- adminer: PHP 기반 — `wget --spider http://localhost:8080/`

**위험**: 이미지에 wget이 없으면 healthcheck 실패 → 컨테이너 unhealthy 마킹. 신중 작업 필요.

---

## 2. 사용자 결정 필요 항목 (Stage 4 진입 전)

`prd2/요청.md` 항목 11~17 — 다음 세션에서 반드시 사용자 답변 필요:

1. **항목 11**: settlement-scheduler 자동화 범위 — OPEN/CALCULATED/CONFIRMED/PAID 중 어디까지 자동? (DEV2-002 선행 결정)
2. **항목 12**: 인정매출 이중 구현 처리 방향 — 통합 vs 구분 (DEV2-004)
3. **항목 13**: members.controller `@Public` 엔드포인트 10개 정리 정책 (DEV1-009)
4. **항목 14**: BONUS-NUANCE-001 "지점 120만" 의미 확정
5. **항목 15**: settlement-scheduler 6-field cron 유지 여부
6. **항목 16**: 다단계 승급 (1단계가 정상 설계인지 확인)
7. **항목 17**: Stage 4 동시성 전략 (전체 병렬 vs 우선순위 순차)

이 7개를 처리하지 않으면 관련 항목 진행 불가 (특히 DEV2-002~004는 항목 11~12 답변 필수).

---

## 3. 남은 76개 항목 분포

| 우선순위 | 미완 | 비고 |
|---------|------|------|
| **P0 Critical** | 18 | DEV1-004(★8 unblock), DEV2-002~003, DEV3-001~003, DESIGN-001~003 |
| **P1 High** | 26 | DEV2-014, DEV1-001~002, DESIGN-006/007, MON-006, DEV3-010 |
| **P2 Medium** | 22 | 다양 |
| **P3 Low** | 10 | 다양 |

### 팀별 진척 (현재)
- 개발3팀 8/18 (44%) — 자동화 인프라 핵심 완성
- 모니터링 4/15 (27%)
- 디자인팀 2/10 (20%)
- 개발2팀 2/21 (10%)
- 개발1팀 1/14 (7%)
- QA 0/14 (0%)

---

## 4. 권장 진행 시퀀스 (전체 Stage 4 완료까지)

```
Phase A (즉시): 1순위 런타임 검증 (~1h)
  ↓ 통과 시
Phase B (subagent 1개): 2순위 DEV1-004 JWT 가드 (~1h)
  ↓ 8 unblock 확인
Phase C (subagent 1개): 3순위 DEV3-001/002 Skill (~1h)
  ↓ 검증 자동화 활성화
Phase D (medium inline): 4순위 DESIGN-001/002/003 (~1h)
Phase E (medium inline): 5순위 MON-006 healthchecks (~30분)
  ↓ 사용자 결정 11~17 답변 받음
Phase F (subagent 2~3개 병렬): DEV2-002~004, DEV1-001/002 등 unblock된 항목들
  ↓
Phase G (subagent 다수): QA 14 항목 (P0 우선) — 테스트 작성 + 실행
  ↓
Phase H (마무리): P2/P3 backlog + 강민호 최종 인수
  ↓
🏁 Stage 4 완료 — 92/92 PASS
```

**전체 추정 wall time**: 8~16h (Phase B/C/F/G가 가장 시간 소요)
**추정 commit 수**: 추가 8~15개

---

## 5. 자율 처리 directive 재확인

이번 세션에서 적용된 directive 계속 유효:
> **"보상플랜 진행 과정에서 필요한 정보 및 요청이 있을경우 임의로 처리 후 /prd2/요청.md에 관련 내용들을 기록해."**

다음 세션에서도 이 원칙 유지:
- 결정 우선순위: ① 첨부 이미지 = source of truth → ② 코드 현실 → ③ PRD 문서
- 결정 사항은 `prd2/요청.md`에 항목 18~ 으로 추가
- 사용자 explicit 결정 필요한 사항만 멈추고 묻기 (요청.md 11~17 같은 정책 결정)

메모리에 저장된 directive: `feedback_autonomous_handling.md` (자동 로드)

---

## 6. 핵심 파일 경로 (재개 시 참조)

### Plan / Spec
- `/home/successbank/.claude/plans/quirky-watching-thacker.md` — 4-stage workflow plan
- `docs/superpowers/specs/2026-04-15-persona-deep-injection-design.md` — 페르소나 spec
- `docs/superpowers/plans/2026-04-15-persona-deep-injection.md` — 페르소나 implementation plan

### Stage 1 (분석)
- `prd/{팀이름}_파악된내용.md` × 10

### Stage 2 (정합화 결과 — commit 9e4305f)
- `.taskmaster/docs/commission-prd.md` (v2.0 신 2종 체계)
- `.taskmaster/docs/prd.md` (v1.1 등급 정정)
- `.claude/CLAUDE.md` (Kaion 도메인 컨텍스트 + v3.1 정정 노트)
- `.claude/personas/*.md` × 10 (브랜드 컬러/등급 정정)
- `apps/backend/src/compensation-plan/services/bonus-simulator.service.ts` (sponsor fix)
- `apps/backend/src/compensation-plan/controllers/compensation-plan.controller.ts` (330만)

### Stage 3 (체크리스트)
- `prd2/{팀이름}_검증체크리스트.md` × 6 (92 항목)
- `prd2/요청.md` (자율 결정 이력)

### Stage 4 진행 (16 PASS)
- `prd2/evidence/{팀}/{ITEM-ID}/{test-result.log,manual.md,diff.patch}` × 16

### Stage 4 신규 작성 파일
- `apps/backend/prisma/migrations/20260415000000_high_freq_price_correction/migration.sql`
- `.claude/settings.json`
- `.husky/pre-commit`, `.husky/commit-msg`

### Stage 4 rename된 .legacy 파일 (보관)
- `apps/backend/src/bonuses/bonus-calculator.service.ts.legacy-old-system`
- `apps/backend/src/compensation-plan/services/bonus-calculator.recognized.spec.ts.legacy-old-system`
- `apps/backend/prisma/seed-commission-rates.ts.legacy-old-system`

### Stage 4 still .bak (Stage 4 BAK-RESTORE-001 대상)
- `apps/backend/src/recognized-sales/recognized-sales.service.spec.ts.bak` (598줄, 25 케이스 회수 가능)
- `apps/backend/src/recognized-sales/recognized-sales.controller.spec.ts.bak` (153줄, 4 케이스)

---

## 7. 위험 및 주의사항

### 🚨 절대 변경 금지
- `apps/backend/src/members/genealogy-raw-queries.ts` — 성능상 raw SQL, 리팩터 절대 금지
- 4개 `.bak` 파일 (이미 처리됨) — 강민호 결정 없이 재변경 금지
- v2 검증 게이트 콘텐츠 (★ 마커, 검증 보고 형식, 완료 선언 금지 조건) — 페르소나 시스템 핵심

### ⚠️ 영향 큰 변경 시 careful work 필요
- `genealogy.service.ts` (raw SQL 호출자)
- `prisma/schema.prisma` (마이그레이션 생성 필요)
- `docker-compose.yml` (컨테이너 재시작 영향)
- `.claude/CLAUDE.md` (페르소나 시스템 전체 영향)

### 🛡️ 미커밋 작업 트리 (그대로 보존)
- `members.controller/service.ts` (사용자 WIP)
- `admin/users/page.tsx`, `BulkPasswordResetModal/`, `member-view/[id]/`
- `services/members.service.ts`
- 루트 `CLAUDE.md` (사용자 수정 중)
- `.persona_team/` 폴더 deletion
- `006 (2).png` (사용자 첨부 이미지)
- `.claude/페르소나_보강_프롬프트_가이드.md`

이 항목들은 본 4-stage workflow와 무관. 절대 commit/edit하지 말 것.

---

## 8. 한 줄 요약

> **다음 세션 시작 시**: ① 런타임 검증 → ② DEV1-004 (JWT 가드, 8 unblock) → ③ DEV3-001/002 (Skill 자동화 허브) → ④ DESIGN-001/002/003 (admin UI) → ⑤ MON-006 (5 healthchecks) → 사용자 답변 11~17 후 Phase F 진입.

---

*작성: PM팀 (강민호 + 박준혁 + 오민정) / 2026-04-15 체크포인트 / 다음 세션 재개용*
